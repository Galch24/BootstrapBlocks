(function ($) {

  $(document).ready(function() {
    
    $('input[data-remote-list]').remoteList({
      minLength: 0,
      /*maxLength: 0,
      param
      source
      select
      renderItem*/
    });
    
  });
  
})(window.jQuery);
